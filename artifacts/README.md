# Artifacts Folder

Download the best-performing Lab 2 workflow artifacts and place them in this folder:

- `trained_model.pkl`
- `results.json`

These files should come from the GitHub Actions run with:

- lowest `MSE`
- highest `R2 Score`

After downloading them from Lab 2, keep the filenames exactly as shown above.

